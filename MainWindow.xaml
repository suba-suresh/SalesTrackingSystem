<Window x:Class="SalesTrackingSystem.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sales Tracker"
        Icon="/Images/logo.ico"
        Height="700" Width="1200"
        MinHeight="600" MinWidth="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <!-- Main outer grid -->
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                   Text="Daily Sales Management"
                   FontSize="26"
                   FontWeight="Bold"
                   Foreground="#2B5797"
                   Margin="0,0,0,15"/>

        <!-- Main Content -->
        <Grid Grid.Row="1" Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" MinWidth="380" MaxWidth="450"/>
                <ColumnDefinition Width="15" />
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Form -->
            <Border Grid.Column="0"
                    Background="White"
                    CornerRadius="10"
                    Padding="20"
                    BorderBrush="#DDD"
                    BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Form Inputs -->
                    <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock Text="Add / Edit Sales"
                                       FontSize="18"
                                       FontWeight="Bold"
                                       Foreground="#2B5797"
                                       Margin="0,0,0,10"/>

                            <!-- Date Input -->
                            <StackPanel Orientation="Horizontal" Margin="0,5">
                                <TextBlock Text="Date:" Width="150" VerticalAlignment="Center"/>
                                <DatePicker SelectedDate="{Binding DateInput}" Width="200"/>
                            </StackPanel>

                            <Separator Margin="0,8"/>

                            <TextBlock Text="Amounts (£)" FontWeight="Bold" Margin="0,8,0,4"/>

                            <!-- Amount Inputs ONLY -->
                            <StackPanel Orientation="Horizontal" Margin="0,5">
                                <TextBlock Text="JustEat:" Width="150" VerticalAlignment="Center"/>
                                <TextBox Text="{Binding AmountJustEatInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="200"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,5">
                                <TextBlock Text="Uber:" Width="150" VerticalAlignment="Center"/>
                                <TextBox Text="{Binding AmountUberInput}" Width="200"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,5">
                                <TextBlock Text="Deliveroo:" Width="150" VerticalAlignment="Center"/>
                                <TextBox Text="{Binding AmountDeliverooInput}" Width="200"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,5">
                                <TextBlock Text="InHouse:" Width="150" VerticalAlignment="Center"/>
                                <TextBox Text="{Binding AmountInHouseInput}" Width="200"/>
                            </StackPanel>

                            <!-- Save/Update/Delete Buttons -->
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                                <Button Content="Save" Command="{Binding SaveCommand}" Width="100" Height="35" Margin="6"
                                        Background="#4CAF50" Foreground="White"
                                        IsEnabled="{Binding CanSave}"/>
                                <Button Content="Update" Command="{Binding UpdateCommand}" Width="100" Height="35" Margin="6"
                                        Background="#2196F3" Foreground="White"
                                        IsEnabled="{Binding CanUpdate}"/>
                                <Button Content="Delete" Command="{Binding DeleteCommand}" Width="100" Height="35" Margin="6"
                                        Background="#F44336" Foreground="White"
                                        IsEnabled="{Binding CanDelete}"/>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- Footer Buttons (Back, View Sales, Clear) -->
                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
                        <Button Content="Back" Command="{Binding BackCommand}" Width="100" Height="35" Margin="4" 
                                Background="#795548" Foreground="White" Cursor="Hand" BorderThickness="0">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="10,8">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <Button Content="View Sales" Command="{Binding ViewSalesCommand}" Width="100" Height="35" Margin="4" 
                                Background="#673AB7" Foreground="White" FontWeight="Bold" Cursor="Hand" BorderThickness="0">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="10,8">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <Button Content="Clear" Command="{Binding ClearCommand}" Width="100" Height="35" Margin="4" 
                                Background="#9E9E9E" Foreground="White" Cursor="Hand" BorderThickness="0">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="10,8">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Right DataGrid -->
            <Border Grid.Column="2" Background="White" CornerRadius="10" BorderBrush="#DDD" BorderThickness="1">
                <Grid>
                    <!-- Loading Overlay -->
                    <Border Background="#80FFFFFF" 
                            Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                            Panel.ZIndex="100">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="Loading sales data..." FontSize="16" FontWeight="Bold" Foreground="#2B5797"/>
                            <ProgressBar IsIndeterminate="True" Width="200" Height="20" Margin="0,10,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- DataGrid with Virtualization -->
                    <DataGrid ItemsSource="{Binding Sales}"
                              SelectedItem="{Binding SelectedSale, Mode=TwoWay}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              Margin="10"
                              AlternatingRowBackground="#FAFAFA"
                              GridLinesVisibility="Horizontal"
                              RowHeight="32"
                              EnableRowVirtualization="True"
                              VirtualizingPanel.IsVirtualizing="True"
                              VirtualizingPanel.VirtualizationMode="Recycling">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=dd/MM/yyyy}" Width="*" />
                            <DataGridTextColumn Header="JustEat Amount (£)" Binding="{Binding AmountJustEat, StringFormat={}{0:F2}}" Width="*" />
                            <DataGridTextColumn Header="Uber Amount (£)" Binding="{Binding AmountUber, StringFormat={}{0:F2}}" Width="*" />
                            <DataGridTextColumn Header="Deliveroo Amount (£)" Binding="{Binding AmountDeliveroo, StringFormat={}{0:F2}}" Width="*" />
                            <DataGridTextColumn Header="InHouse Amount (£)" Binding="{Binding AmountInhouse, StringFormat={}{0:F2}}" Width="*" />
                            <DataGridTextColumn Header="Total (£)" Binding="{Binding TotalAmount, StringFormat={}{0:F2}}" Width="*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
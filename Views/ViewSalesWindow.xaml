<Window x:Class="SalesTrackingSystem.Views.ViewSalesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:SalesTrackingSystem.ViewModels"
        mc:Ignorable="d"
        Title="Sales Analytics"
        Height="650" Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="#FAFAFA">

    <Window.Resources>
        <Style x:Key="FilterButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="8,2"/>
            <Setter Property="Margin" Value="0,0,0,0"/>
            <Setter Property="BorderBrush" Value="#CCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="Auto"/>
            <!-- Controls -->
            <RowDefinition Height="Auto"/>
            <!-- Summary -->
            <RowDefinition Height="*"/>
            <!-- DataGrid -->
            <RowDefinition Height="Auto"/>
            <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <TextBlock Grid.Row="0" Text="Sales Analytics Dashboard"
                   FontSize="24" FontWeight="Bold"
                   Foreground="#222" />

        <TextBlock Grid.Row="0" Text="— view daily/monthly/yearly reports"
                   FontSize="14" Foreground="Gray"
                   Margin="290,5,0,0"/>

        <!-- CONTROL BAR -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,20,0,20" VerticalAlignment="Center" >
            <TextBlock Text="Select Date:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <DatePicker SelectedDate="{Binding SelectedDate}" Width="150"/>

            <Button Content="Daily View" Command="{Binding DailyViewCommand}" Style="{StaticResource FilterButtonStyle}" Margin="10,0"/>
            <Button Content="Monthly View" Command="{Binding MonthlyViewCommand}" Style="{StaticResource FilterButtonStyle}" Margin="10,0"/>
            <Button Content="Yearly View" Command="{Binding YearlyViewCommand}" Style="{StaticResource FilterButtonStyle}" Margin="10,0"/>
            <Button Content="Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource FilterButtonStyle}" Margin="10,0"/>
            <Button Content="Print Grid" Command="{Binding PrintCommand}"
                    Background="#E91E63" Foreground="White" Margin="20,0,0,0" Width="120" Height="30" FontWeight="Bold"/>
        </StackPanel>

        <!-- SUMMARY CARDS -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
            <Border Background="White" CornerRadius="10" Padding="20" Margin="10" Width="250" BorderBrush="#DDD" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Total Orders" FontWeight="Bold" FontSize="14" Foreground="#555"/>
                    <TextBlock Text="{Binding TotalOrders}" FontSize="22" FontWeight="Bold" Foreground="#333"/>
                </StackPanel>
            </Border>

            <Border Background="White" CornerRadius="10" Padding="20" Margin="10" Width="250" BorderBrush="#DDD" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Total Revenue" FontWeight="Bold" FontSize="14" Foreground="#555"/>
                    <TextBlock Text="{Binding TotalRevenue, StringFormat=£{0:F2}}" FontSize="22" FontWeight="Bold" Foreground="#333"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- DATAGRID -->
        <DataGrid x:Name="SalesDataGrid" Grid.Row="3"
                  ItemsSource="{Binding FilteredSales}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  Margin="0,20,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=d}" Width="*" />
                <DataGridTextColumn Header="JustEat (orders)" Binding="{Binding OrdersJustEat}" Width="*" />
                <DataGridTextColumn Header="Uber (orders)" Binding="{Binding OrdersUber}" Width="*" />
                <DataGridTextColumn Header="Deliveroo (orders)" Binding="{Binding OrdersDeliveroo}" Width="*" />
                <DataGridTextColumn Header="InHouse (orders)" Binding="{Binding OrdersInHouse}" Width="*" />
                <DataGridTextColumn Header="JustEat (£)" Binding="{Binding AmountJustEat, StringFormat=£{0:F2}}" Width="*" />
                <DataGridTextColumn Header="Uber (£)" Binding="{Binding AmountUber, StringFormat=£{0:F2}}" Width="*" />
                <DataGridTextColumn Header="Deliveroo (£)" Binding="{Binding AmountDeliveroo, StringFormat=£{0:F2}}" Width="*" />
                <DataGridTextColumn Header="InHouse (£)" Binding="{Binding AmountInhouse, StringFormat=£{0:F2}}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- FOOTER -->
        <Button Grid.Row="4" Content="Exit"
                Width="100" Height="35"
                HorizontalAlignment="Right"
                Margin="0,10,10,0"
                Background="#444" Foreground="White"
                Click="Exit_Click"/>
    </Grid>
</Window>

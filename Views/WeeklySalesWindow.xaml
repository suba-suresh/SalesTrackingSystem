<Window x:Class="SalesTrackingSystem.Views.WeeklySalesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Weekly Sales Report" Height="500" Width="600">

    <DockPanel Margin="10">
        <!-- Date Picker -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Pick a date:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <DatePicker SelectedDate="{Binding SelectedDate, Mode=TwoWay}"/>
            <Button Content="Load Week" Command="{Binding LoadWeekCommand}" Margin="10,0,0,0"/>
        </StackPanel>

        <!-- Weekly Summary Grid -->
        <DataGrid ItemsSource="{Binding WeeklySummary}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  Margin="0,10,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Day" Binding="{Binding Day}" Width="*"/>
                <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="*"/>
                <DataGridTextColumn Header="Total Orders" Binding="{Binding TotalOrders}" Width="*"/>
                <DataGridTextColumn Header="Total Amount" 
                                    Binding="{Binding TotalAmount, StringFormat={}{0:C2}, ConverterCulture=en-GB}" 
                                    Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

    </DockPanel>
</Window>
